import React from 'react';

class ContentRightMidle extends React.Component {

    render() {
        const website = `http://${window.location.hostname}:${window.location.port}/`;

        return (
            <div>
              Votre contenu ici
                </div>
        );
    }
}

export default ContentRightMidle;


















